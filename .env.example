# 复制成 .env 并填入你的 key，然后 chmod 600 .env
# .env 已在 .gitignore 里，不会进版本库

# LLM（任选一家，Gemini 免费额度是需求的 250 倍）
GEMINI_API_KEY=
# DEEPSEEK_API_KEY=
# ZHIPU_API_KEY=
# XAI_API_KEY=          # 可官方检索 X，替代爬虫

# 面板访问口令。绑 127.0.0.1 时可留空；
# 一旦 --host 0.0.0.0 对外提供服务，不设这个会拒绝启动。
# PANEL_TOKEN=
